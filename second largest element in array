class Solution:
    def getSecondLargest(self, arr):
        
        n=len(arr)
        arr.sort()
        largest=arr[n-1]
        for i in range(n-2,-1,-1):
            if(arr[i]!=largest):
                return arr[i]
            
        return -1    
